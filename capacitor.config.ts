import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simpleai.twentyonequestions',
  appName: '21 Questions',
  webDir: 'www'
};

export default config;
